[project]
name = "math-game"
version = "0.1.0"
description = "Domain contracts for a comparable math game"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
